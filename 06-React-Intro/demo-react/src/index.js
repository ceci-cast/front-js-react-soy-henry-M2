import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root')); //se trae un elemento que el id es root
root.render(    // en eso que me traje de html renderiza (mostrar en pantalla)
  
    <App />
 
);


